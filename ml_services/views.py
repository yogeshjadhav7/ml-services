from django.views.decorators.csrf import csrf_exemptfrom django.http import JsonResponseimport numpy as npimport jsondef default(o):    if isinstance(o, np.int64): return int(o)    raise TypeError@csrf_exemptdef landing(request):    return JsonResponse('Hello! This is my ML service project which will serve Machine Learning services to all my projects. Real training and predictions happen here which flow to respective projects via REST calls :)',                        safe=False)